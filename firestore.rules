rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // 🔒 SIKKERHET: Standard - Ingen tilgang uten autentisering
    // Denne appen bruker ikke Firestore per nå, så vi stenger alt
    match /{document=**} {
      allow read, write: if false;
    }
    
    // 📝 FREMTIDIG: Hvis du skal bruke Firestore senere, 
    // erstatt "false" med faktisk autentiseringslogikk:
    // allow read, write: if request.auth != null;
    
    // Eksempel for sjekkliste-lagring (kun for referanse):
    // match /checklists/{checklistId} {
    //   // Kun autentiserte brukere kan lese sine egne sjekklister
    //   allow read: if request.auth != null && request.auth.uid == resource.data.userId;
    //   
    //   // Kun autentiserte brukere kan opprette/oppdatere sine egne sjekklister
    //   allow create, update: if request.auth != null && request.auth.uid == request.resource.data.userId;
    //   
    //   // Kun eieren kan slette
    //   allow delete: if request.auth != null && request.auth.uid == resource.data.userId;
    // }
  }
}
